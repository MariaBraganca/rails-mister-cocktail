<%= simple_form_for [@cocktail, @dose] do |f| %>
	<%= f.input :description, label: "Set the measure " %>
	<%= f.association :ingredient, collection: Ingredient.order(:name), label: "Choose an ingredient " %>
	<%= f.submit 'Add', :class => "btn btn-add w-100" %>
<% end %>