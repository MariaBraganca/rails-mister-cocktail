<%= simple_form_for(@cocktail) do |f| %>
    <%= f.input :category, collection: $categories %>
    <%= f.input :alcoholic, collection: $alcoholics %>
    <%= f.input :glass, collection: $glasses %>
	<%= f.submit 'Edit', :class => "btn btn-add w-100" %>
<% end %>